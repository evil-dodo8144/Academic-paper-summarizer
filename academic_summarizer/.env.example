# Copy to .env and set your values.
# Configure either ScaleDown (preferred if both are set) or Groq.

# ScaleDown (no trailing slash).
# SCALEDOWN_BASE_URL=https://api.scaledown.xyz/v1
# SCALEDOWN_API_KEY=your_scaledown_key_here
# Set to 1 only if your ScaleDown endpoint uses x-api-key instead of Bearer.
# SCALEDOWN_USE_X_API_KEY=0

# Groq (OpenAI-compatible API surface; no trailing slash).
# GROQ_BASE_URL=https://api.groq.com/openai/v1
# GROQ_API_KEY=your_groq_key_here

# Optional: ScaleDown compress API (for chunk compression). Uses x-api-key.
# When set, chunk compression uses this endpoint instead of the LLM.
# SCALEDOWN_COMPRESS_URL=https://api.scaledown.xyz/compress/raw/