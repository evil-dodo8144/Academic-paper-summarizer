# Copy to .env and set your values.
# For summarization use OpenAI; ScaleDown.xyz often returns 403 for chat.

# Chat completions base URL (no trailing slash).
SCALEDOWN_BASE_URL=https://api.openai.com/v1

# API key: use an OpenAI key for chat (https://platform.openai.com/api-keys)
SCALEDOWN_API_KEY=YIhY1dwaMccXZNnhQyLM1tywrwDvXwt6gU6VWoM3

# Set to 1 only if your API uses x-api-key instead of Bearer.
# SCALEDOWN_USE_X_API_KEY=0

# Optional: ScaleDown compress API (for chunk compression). Uses x-api-key.
# When set, chunk compression uses this endpoint instead of the LLM.
# SCALEDOWN_COMPRESS_URL=https://api.scaledown.xyz/compress/raw/
