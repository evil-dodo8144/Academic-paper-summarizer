# ScaleDown / OpenAI-compatible API. Copy to .env and set your values.

# Chat completions (for summarization). No trailing slash.
# Examples: https://api.openai.com/v1  or  https://api.scaledown.ai/v1
SCALEDOWN_BASE_URL=https://api.openai.com/v1

# API key (used for both chat and compress if set)
SCALEDOWN_API_KEY=YIhY1dwaMccXZNnhQyLM1tywrwDvXwt6gU6VWoM3

# Optional: use x-api-key header instead of Bearer (e.g. for ScaleDown)
# SCALEDOWN_USE_X_API_KEY=1

# Optional: ScaleDown compress API (for chunk compression). Uses x-api-key.
# When set, chunk compression uses this endpoint instead of the LLM.
# SCALEDOWN_COMPRESS_URL=https://api.scaledown.xyz/compress/raw/
